# GitGym

GitGym is a desktop-first web sandbox for practicing Git commands against disposable repositories.

This repository currently contains the root bootstrap and placeholder workspace targets for planned components.

## Services

- `apps/web`: planned React workbench scaffold
- `services/api`: planned browser-facing API and auth service scaffold
- `services/runner`: planned workspace and Git execution service scaffold
